CREATE TRIGGER Feedback_CHANGED
ON FeedBack
AFTER INSERT, UPDATE, DELETE
AS
BEGIN
  TRUNCATE TABLE RouteRate;
  INSERT INTO RouteRate SELECT IdRoute, AVG(CAST(Rating AS DECIMAL(2,1)))  FROM [Turisma].[dbo].[FeedBack] GROUP BY IdRoute;
END